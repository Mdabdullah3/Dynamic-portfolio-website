// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "digitalnomadshq.com.au", // ← ADD THIS
    ],
  },
};

export default nextConfig;
